{
    "version": 2,
    "name": "inextenso",
    "builds": [
        { "src": "functions/**/*.ts", "use": "@now/node" }
    ],
    "build": {
        "env": {
            "REDIS_URL": "@redis_url",
            "REDIS_PSW": "@redis_psw"
        }
    },
    "env": {
        "REDIS_URL": "@redis_url",
        "REDIS_PSW": "@redis_psw"
    },
    "routes": [
        { "src": "/(.*)", "dest": "/functions/$1" }
      ]
}
